apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: {{ include "flask.name" . }}-role
  namespace: {{ .Values.Namespace }}

rules:

- apiGroups:
  - ""

  resources:
  - configmaps

  verbs:
  - get
  - list